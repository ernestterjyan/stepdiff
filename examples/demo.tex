\documentclass{article}

\usepackage[margin=1in]{geometry}
\usepackage{xcolor}
\usepackage{stepdiff}

\title{stepdiff v1.0.0-rc1 Demo}
\author{}
\date{}

\stepdiffsetup{theme=soft, layout=lecture, highlight=background}

\begin{document}

\maketitle

\section*{Algebra expansion}

\[
\begin{stepdiff}
\step{a(x+b)}
\step[reason={distribute}]{ax + ab}
\step[reason={commute terms}]{ab + ax}
\end{stepdiff}
\]

\section*{Relation-aware derivation}

\[
\begin{stepdiff}
\step{a_n \le b_n}
\step[reason={take limits}]{\lim a_n \le \lim b_n}
\step[reason={conclude}, diff=all]{L \le M}
\end{stepdiff}
\]

\section*{Derivative calculation}

\[
\begin{stepdiff}[layout=compact]
\step{f(x) = x^3 + 2x}
\step[reason={differentiate}]{f'(x) = 3x^2 + 2}
\step[reason={evaluate at x=2}]{f'(2) = 3\cdot 2^2 + 2}
\step[reason={simplify}]{f'(2) = 14}
\end{stepdiff}
\]

\section*{Manual control}

\[
\begin{stepdiff}[theme=minimal, highlight=underline]
\step{x^2 - 1 = (x-1)(x+1)}
\step[reason={automatic diff}, diff=auto]{x^2 - 1 = x^2 - 1}
\step[reason={suppress noisy highlight}, diff=false]{x^2 - 1 = x^2 - 1}
\step[reason={emphasize identity}, diff=all]{x^2 - 1 = (x-1)(x+1)}
\end{stepdiff}
\]

\section*{Customization hook}

\begingroup
\renewcommand{\SDchanged}[1]{\begingroup\color{red!70!black}#1\endgroup}
\[
\begin{stepdiff}
\step{p(x) = x^2 + 2x + 1}
\step[reason={custom highlight style}]{p(x) = (x+1)^2}
\end{stepdiff}
\]
\endgroup

\end{document}
